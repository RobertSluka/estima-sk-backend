# Secrets — never commit
.env
apify_runner/.env

# Scraped data dumps (regenerated by the runner; keep the folder, not the files)
data/*.json

# Generated model artifacts + training CSVs (regenerated by `train`)
artifacts/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Logs
*.log
apify_runner/cron.log

# OS / editor
.DS_Store

# pytest cache
.pytest_cache/

# Cached OSM static maps / POI counts for report location sections (regenerated on demand)
data/location_maps/
data/location_pois/
